<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verse vault</title>
    <link rel="stylesheet" href="/styles/main.css">

</head>
<body>
    
  <div class="container">
    
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" />
    <form class="navbar-form" role="search" action="/" method="POST">
        <div class="input-group add-on">
        <input class="form-control" placeholder="Artist" name="artist" id="srch-term1" type="text">
        <input class="form-control" placeholder="Title" name="title" id="srch-term2" type="text">
            <div class="input-group-btn">
                <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
            </div>
        </div>
        </form>

        <!-- Display lyrics if available -->
        <% if (locals.lyrics) { %>
            <h2>Lyrics:</h2>
            <pre><%= lyrics.lyrics %></pre>
        <% } else { %>
            <p>No lyrics found for the given artist and title.</p>
        <% } %>
    </div>      
</body>
</html>